@node_modules/@schlessera/brain/CONTRACT.md

# my-brain

<!-- brain:generated:bootstrap -->
This is an **uninitialized brain**. It has the default structure (`me/`,
`context/`, `notes/`) and one example note, but no personalized taxonomy yet.

**Next step:** run **`/brain-init`**. The interview asks a few questions about
you and what you want to track, then generates your directory taxonomy, seeds
`me/identity.md`, registers the MCP server, and rewrites this file's generated
sections.

Until then the CLI already works: `brain add "a thought"` captures a note and
`brain search "hello"` finds the example note in `notes/`.
<!-- /brain:generated:bootstrap -->

<!--
Everything OUTSIDE the brain:generated markers above is yours. /brain-init and
/brain-module only rewrite the marked regions; anything you add here is kept
verbatim across regenerations. Put your own house rules, links, and notes below.
-->
